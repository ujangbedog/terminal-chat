# Simple Chat Application Configuration
# Copy this file to .env and modify as needed

# Server configuration
RUST_LOG=info
SERVER_HOST=127.0.0.1
SERVER_PORT=8080

# Client configuration  
CLIENT_MODE=1  # 1 for simple mode, 2 for interactive mode

# Logging levels:
# - error: Only errors
# - warn: Warnings and errors  
# - info: Info, warnings, and errors (recommended)
# - debug: Debug info and above
# - trace: All logging (very verbose)

# Example for debug logging:
# RUST_LOG=debug

# Example for server-only debug logging:
# RUST_LOG=chat_server=debug
